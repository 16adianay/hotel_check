import React from 'react';
import {Link, Route} from "react-router-dom";

const Main = () => {
    return (
        <Route path={'/'}>
            <p>Hello</p>
        </Route>
    );
};

export default Main;